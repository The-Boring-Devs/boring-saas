<%= render 'shared/center_box' do %>
  <h1 class="text-xl font-bold leading-tight tracking-tight text-gray-900 md:text-2xl dark:text-white">
    Recover your password
  </h1>

  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: 'space-y-4 md:space-y-6' }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>
    <div>
      <%= f.label :email, class: label_field_classes(f, :email) %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: input_field_classes(f, :email) %>
      <%= error_hint(f, :email) %>
    </div>

    <%= f.submit "Send me reset password instructions", class: "w-full text-white bg-primary-600 hover:bg-primary-700 focus:ring-4 focus:outline-none focus:ring-primary-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-primary-600 dark:hover:bg-primary-700 dark:focus:ring-primary-800" %>
  <% end %>
<% end %>
